import { Text, View } from 'react-native';

export function Map() {
  return (
    <View className="flex-1 items-center justify-center p-6">
      <Text className="text-center text-sm font-semibold leading-5 text-ink">
        Mapbox map is available in the native app.
      </Text>
    </View>
  );
}
