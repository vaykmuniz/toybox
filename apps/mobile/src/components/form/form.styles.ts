export const formInputClasses = {
  error: 'font-display text-xs font-bold text-red-600',
  input:
    'min-h-12 rounded-2xl border border-theme-selected bg-white px-4 py-3 font-display text-base font-semibold text-ink',
  label: 'font-display text-sm font-black text-ink',
  passwordContainer:
    'min-h-12 flex-row items-center rounded-2xl border border-theme-selected bg-white',
  passwordInput: 'min-h-12 flex-1 px-4 py-3 font-display text-base font-semibold text-ink',
  toggle: 'px-4 py-3',
  toggleText: 'font-display text-sm font-black text-toybox-blue',
  wrapper: 'gap-2',
};
